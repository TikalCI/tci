seed_jobs:
  test-tciGeneral:
    source:
      remote: git@github.com:TikalCI/tci-pipelines.git
      branch: '${TCI_PIPELINES_BRANCH}'
      credentialsId: gitsshkey

    pipeline: src/test/tciGeneral/Jenkinsfile
    executeWhen: never

  test-tciOverall:
    source:
      remote: git@github.com:TikalCI/tci-pipelines.git
      branch: '${TCI_PIPELINES_BRANCH}'
      credentialsId: gitsshkey

    pipeline: src/test/tciOverallSimple/Jenkinsfile
    executeWhen: never
